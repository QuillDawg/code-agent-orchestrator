interface:
  display_name: "CAO YAML"
  short_description: "Author and validate a CAO workflow YAML"
  default_prompt: "Use $cao-yaml to create a CAO workflow for this request."
policy:
  allow_implicit_invocation: false
